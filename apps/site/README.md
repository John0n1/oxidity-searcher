# Oxidity Site

Marketing and download site for `oxidity.io`.

## Local development

1. Install dependencies: `npm install`
2. Start the dev server: `npm run dev`

## Production build

- Type-check: `npm run lint`
- Build: `npm run build`

The production output is written to `dist/` and is published to `/var/www/oxidity.io`.
